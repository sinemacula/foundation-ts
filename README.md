# @sinemacula/foundation

Platform-agnostic TypeScript foundation: the framework-free ports and logic (http, config, storage, query, realtime, authorization, logging, i18n) shared by Sine Macula's web and mobile kernels.

> Status: under active extraction from `web-core-ts`. Tracked on the `wip/split` branch; `master` stays clean until the split lands. Not yet published.
