import typeChecked from "@sinemacula/coding-standards/js/eslint/type-checked";

// This is a framework-agnostic TypeScript package, so it runs the type-aware
// layer (which spreads the base) with no framework layer on top.
export default [...typeChecked];
